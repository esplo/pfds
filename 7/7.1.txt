回転時に作られる停止計算の数は、最大時で以下のようになる。

|f|, |r| => |停止計算| とすると、
0, 1 => 1
1, 2 => 2
3, 4 => 3
7, 8 => 4
15, 16 => 5
...

|停止計算| ~= log(|f| + |r|)

また、停止計算の本質的コストはO(1)なので、計算量は進行させる個数に依存する。

これより、停止計算の連鎖は最大でlog(n)であり、最悪ケースの実行時間はO(log(n))になる。
